import styled from 'styled-components';

export const Wrapper = styled.ul`
  max-width: 300px;
  width: 100%;
  margin-inline: auto;
  list-style: none;
  margin-top: 100px;
  padding: 0;
  font-size: 24px;
`;
